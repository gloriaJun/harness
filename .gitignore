# Backup files created by setup.sh
*.backup.*

# OS
.DS_Store

# Generated checksum files
hooks/*.sha256

# Secret-containing files (if accidentally copied)
codex/config.toml

# Codex auto-saved session rules (may contain internal URLs)
codex/rules/default.rules
